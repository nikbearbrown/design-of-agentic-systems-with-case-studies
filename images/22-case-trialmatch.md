# Image brief — Ch 22 Case: TrialMatch AI

**Filename:** `images/22-case-trialmatch.jpg`
**Aspect:** 16:9 hero, Kindle-readable.

**Subject:** Eight agent boxes arrayed around a central Controller node. Three input modalities feeding the upper-left cluster (text / voice / image), Agents 1-3 visibly running in parallel. Agents 4-7 in a sequential chain on the right. Above each Parser/NER agent, a small badge indicating "LoRA fine-tuned" vs "LLM fallback" — the Controller's Model Selector arrow visible.

**Mood:** Diagnostic, schematic, government-API-driven. Carefully distinct from a marketing visual.

**Negative space:** Top for chapter title; bottom for the per-criterion audit-trail caption.

**Notes:** The teaching image is *the Controller chooses the model at runtime.* Make the LoRA/LLM selector arrow the most visually prominent design element. Avoid medical imagery clichés (DNA helix, pill bottles).
